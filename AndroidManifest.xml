<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="training.unrc"
    android:versionCode="2"
    android:versionName="unrc 2.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="training.unrc.InitialActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="training.careers.CareersActivity"
            android:label="@string/title_activity_main_careers" >
        </activity>
        <activity
            android:name="training.map.MapActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="training.bus.BusActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="training.bus.BusSchedules"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="training.bus.PathAndSchedulesButtons"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Dialog" >
        </activity>
        <activity
            android:name="training.bus.PathBus"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="training.bus.AddSchedule"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="training.unrc.FacebookActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name="training.bus.DeleteSchedule" >
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" />

        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>